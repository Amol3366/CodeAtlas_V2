# notifications-mixed (fixture)

A small multi-language repository used to exercise CodeAtlas file classification
and multi-language scanning. Contains:

- Python backend: `NotificationService.send` — `backend/app.py`
- TypeScript frontend: `NotificationClient.notify` — `frontend/client.ts`
- Configuration in YAML, JSON, and TOML: `config/app.yaml`, `config/app.json`,
  `pyproject.toml`

Fixture data only.
