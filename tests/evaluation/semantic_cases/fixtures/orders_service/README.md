# Orders service

A small ordering service: baskets, pricing, stock, and notifications.

## Running

There is nothing to run. This tree is fixture content for evaluation and is
never executed.

## Layout

- `src/orders/` — the domain and the service that sequences it
- `tests/` — behaviour that has to keep working
- `docs/` — architecture notes and the on-call runbook
