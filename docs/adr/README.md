# Architecture Decision Records

ADRs capture decisions that affect product scope, privacy, trust, compatibility,
security, persistence, deployment, or an established public contract.

## Workflow

1. Copy `0000-template.md` to the next four-digit number and a short slug.
2. Set status to `proposed`; describe context, decision, alternatives,
   consequences, migration/rollback, security/privacy effects, and approval.
3. Link supporting measurements or discovery evidence.
4. Obtain explicit user/product approval for decisions listed in `AGENTS.md`
   Section 25.
5. Set status to `accepted` only after approval. Implementation plans may then
   depend on it.
6. Never edit an accepted decision to change its meaning. Add a new ADR with
   status `supersedes ADR-NNNN`.

ADR timestamps use UTC dates. Rejected and superseded records remain for audit.

## Accepted records

| ADR | Decision | Phase |
| --- | --- | --- |
| [0001](0001-local-deterministic-modular-monolith.md) | Local-first deterministic modular monolith; SQLite as the system of record | 0 |
| [0002](0002-phase1-storage-and-migration-mechanism.md) | Storage layout and the explicit forward-only migration mechanism | 1 |
| [0003](0003-evidence-granularity.md) | Evidence granularity; the gate is measured on containing evidence, and the corpus is never edited to fit the engine | 3 |
| [0004](0004-relation-model-and-contract-additions.md) | Relation model, derivation classes, and the additive contract entries | 3 |
| [0005](0005-change-assurance-engine-design.md) | Change-assurance engine: state views, impact orientation, finding rules, risk ordering | 4 |
| [0006](0006-web-application-design.md) | Web application: persistence, streaming, sanitization, and evidence presentation | 5 |
| [0007](0007-freshness-and-hardening-design.md) | The watcher is a trigger, never an authority; recovery, backup, and packaging decisions | 6 |
| [0008](0008-accept-then-stream-message-submission.md) | Accept-then-stream message submission; `contract_version` 1.0 → 1.1 | 6 |
| [0009](0009-measured-semantic-uplift.md) | Optional semantic layer admitted: provider-neutral embeddings, LanceDB base/delta with SQLite membership authoritative, per-repository privacy governance, shadow migration, measurement-admitted rerank/explanation | 7 |
| [0010](0010-repository-scoped-embedding-namespaces.md) | Which similarity space answers is a per-repository pointer, not a global active flag; migration `0012` drops the one-active index and backfills existing databases | 7 (post-gate) |
| [0011](0011-configurable-embedding-models.md) | Embedding model identity is configurable through `.env`; namespace derivation keeps it safe, and a custom OpenAI model must declare its width | 7 (post-gate) |
| [0012](0012-governed-answer-provider-policy.md) | Answer generation writes prose over untouched claims and evidence; local `llama3.2:3b` is primary, the default is off, and the feature ships available rather than admitted | 7 (post-gate) |
| [0013](0013-ephemeral-session-mode.md) | Ephemeral sessions are opt-in and never the default; one injected database path makes indexing, embeddings, and storage fresh per run, and §8.2's persistence requirement is scoped to default mode | none (post-gate) |
| [0014](0014-per-repository-embedding-model.md) | The embedding model is a per-repository decision for the local provider; migration `0014` stores it, precedence is policy → `.env` → default, and a candidate model's width is measured rather than declared | none (post-gate) |
| [0015](0015-frontend-credential-entry.md) | The OpenAI API key is entered in Settings and stored in the Windows Credential Manager, machine-wide, precedence store → `.env`; no response carries the value or any part of it, and the resolved key is never published to `os.environ` | none (post-gate) |
| [0016](0016-derivation-tiered-test-edges.md) | `TESTS` is derivation-tiered — direct edges stay `high_confidence_heuristic`, fixture- and helper-mediated edges are `low_confidence_heuristic`; `CONSUMES_FIXTURE` is stored and citable but excluded from impact expansion, and a weak edge explains a `test_gaps` entry without ever closing it | none (post-gate) |
| [0017](0017-evaluation-fixture-gate-correction.md) | `SUPPORTED_FIXTURES` had been frozen since Phase 1, scoring 16 of 39 query cases as misses the engine never saw; widening it to every fixture but `malicious_unsupported` moved `exact_symbol_resolution` 0.3846 → 0.6154 and `abstention_correctness` 0.5250 → 0.7500, regenerating the two live baselines while Phase 1–2 stay frozen as history — **corrected by ADR-0018** | none (post-gate) |
| [0018](0018-graph-query-subject.md) | A graph case declares `query_subject`, because `expected_symbols` is the answer and the subject is not in it; six cases declare it, the subject named is the one the question asks rather than the one the engine answers, and the module-symbol ranking and `related_tests` method-resolution findings it exposes are deferred rather than bundled | none (post-gate) |
| [0019](0019-export-evidence-labelling.md) | Evidence is labelled with the symbol whose definition its cited range covers — `EXPORTS` cites the exported symbol, every other kind cites a reference site inside the source; fixes an evidence object that named one symbol and showed another, with the `IMPORTS` counterpart pinned so the fix cannot flip it too | none (post-gate) |
| [0020](0020-relations-in-every-graph-answer.md) | Every graph answer populates `relation_paths`, not just `trace` — the traversal already computed them and `_respond` discarded them, leaving an MCP client with prose and no machine-readable statement of what relates to what; also revives `relation_path_correctness`, a metric structurally stuck at 0.0000 since Phase 3 | none (post-gate) |
| [0021](0021-method-level-test-edges.md) | A method is never imported, so no method could ever carry a `TESTS` edge — import-and-call now applies at the right granularity (imported **class**, resolved call to its method, `static_resolved`), fixing a false `test_gaps` entry on the most common shape of Python test; the owner must be a class, never a module, a constraint the ADR-0016 invariant corpus caught | none (post-gate) |
| [0022](0022-corpus-line-endings.md) | One corpus variant file held CRLF in the working tree, so every line of it differed and all five of its functions reported as changed against a case declaring one; `baseline-phase-7` encoded that drift and exited 5 on a correctly-checked-out tree. Restoring the file takes `changed_symbol_precision` 0.2000 → 1.0000, leaving Phase 7 with three unmet targets rather than four | none (post-gate) |
| [0023](0023-target-profiles.md) | A dataset declares a `target_profile`, so a 14-case conceptual corpus is no longer held to a 0.98 top-1 rule written for exact symbol lookup; `exact_symbol_resolution` is scoped to symbol-shaped intents (1.0000) with a new **failing** `lexical_resolution` gate (0.3000) as the condition that keeps the scoping honest; the evidence gate reads `containing_evidence_rate` with its 1.0 threshold unchanged | none (post-gate) |
| [0024](0024-unmeasured-is-not-wrong.md) | The adapter promised that "not implemented" and "answered wrongly" stay different facts; the scorer blurred them, so cases on a deliberately excluded fixture counted as misses and put an unreachable 0.80 ceiling under a 0.90 gate. `QueryPrediction.measured` carries the distinction and unmeasured cases leave every accuracy aggregate — including `abstention_correctness`, which was crediting the engine for decisions it never made | none (post-gate) |
| [0025](0025-nested-configuration-keys.md) | `_nested_paths` computed `service.port` and friends and then flattened them into a display string, so a nested key was searchable prose but not an addressable symbol and a config lookup could only answer the parent; each leaf now cites the line that sets it, with a text-matched line that falls back to the parent's range rather than being guessed | none (post-gate) |
| [0026](0026-exact-match-ranking.md) | Ranking was pure BM25, so a short parent block out-scored the leaf a caller asked for by name while a longer one did not — whether you got your key or its parent depended on the parent's length; an exact `qualified_name` match is now promoted within the returned window, as a stable partition that leaves every other query's order untouched | none (post-gate) |

| [0027](0027-containing-evidence-recall.md) | `primary_evidence_recall_at_10` compared line ranges for exact equality, so a citation one line longer than the gold range scored as never found — four of Phase 7's five misses return the right evidence at ranks 1, 1, 2 and 4. ADR-0003's containment predicate now backs a `containing_evidence_recall_at_10` that takes the gate at an unchanged 0.90 threshold, with the exact-match number retained so no historical figure changes meaning. **No engine behaviour changed** | none (post-gate) |

| [0028](0028-rank-fusion.md) | Fusion appended semantic candidates after all deterministic evidence and dropped any already cited, so a chunk both channels found kept its lexical position — the semantic channel ranked s007's answer 8th and s003's 1st while the fused answer buried them at 16th and 5th, and **two separately-recorded engine defects turned out to be one fusion defect**. Reciprocal-rank fusion over both channels, ranks only and never scores; Recall@10 1.0000 and MRR 0.4429 → 0.6875 with evidence rates unchanged, the signature of a pure reorder | none (post-gate) |

| [0029](0029-memberless-container-chunks.md) | A class chunk is an outline naming its members, which is right until the class has no member *symbols* — an enum's values are assignments, so `OrderStatus` was indexed as `class OrderStatus(Enum):` and nothing else, with `DRAFT`/`PLACED`/`SHIPPED`/`CANCELLED` and its docstring absent from the index entirely. A container with no members is a leaf and carries its body; `CHUNKER_VERSION` 1.0.0 → 1.1.0, its first move since Phase 2, so every snapshot must be re-indexed | none (post-gate) |

| [0030](0030-conceptual-answers-at-module-granularity.md) | s001's last miss is not a defect: the module docstring "Keeping two customers from being sold the same unit" paraphrases the question, so both channels rank the module first and are right to, while the corpus declares the method that implements it. **Nothing is changed** — the coarse-chunk penalty that would promote the method also demotes the chunk currently providing the rank-1 containment hit, so it trades an evidence hit for a symbol hit and needs corpus-wide measurement, not one case | none (post-gate) |

| [0031](0031-document-section-naming.md) | The corpus used two conventions for naming a markdown section — q019 declared `README.Health` while q027/q031 declared bare headings and extraction emits bare headings everywhere. Because `expected_symbols[0]` is **the query the harness issues**, q019 was asking for a symbol nothing can produce and the engine's correct abstention was scored as a miss. Bare heading is now the single rule; `lexical_resolution` 0.8750 → 1.0000 and `abstention_correctness` → 1.0000, from one line — the leverage ADR-0003 restrains, justified by the corpus contradicting itself rather than by the number | none (post-gate) |

| [0032](0032-lexical-resolution-threshold.md) | `lexical_resolution` scores **eight** cases (two of ten are excluded by ADR-0024), so it moves in steps of 0.125 and the provisional 0.90 already required 8/8 with zero failures tolerated — arithmetically identical to 1.0 while reading as though a miss were acceptable. Set to 1.0; both baselines reproduce byte-for-byte, which is the evidence it is a restatement not a tightening. **`exact_symbol_resolution`'s 0.98 has the same illusion** — 27 cases, 27/27 required — and is left open because it is a Section 19.3 target cited in approved gates | none (post-gate) |

| [0033](0033-exact-symbol-threshold-granularity.md) | The second instance ADR-0032 recorded: `exact_symbol_resolution` scores 27 cases against 0.98, requiring 27/27 with zero failures tolerated. **Kept at 0.98, deliberately not restated as 1.0** — unlike `lexical_resolution`'s internal provisional 0.90, this is a Section 19.3 release target that becomes expressible at ~50 cases, so restating it would tighten a product promise to match an artifact of corpus size. Documented at the constant and pinned by tests; the real fix is corpus size | none (post-gate) |

| [0034](0034-trace-follows-routes.md) | `relation_path_correctness` (0.3182, no gate target) averages **four unrelated causes**, which is why no threshold could mean anything. Fixes one: `trace` never traversed `ROUTES_TO`, so a flow could not cross the HTTP boundary that relation exists to model, and an answer with edges but no buildable path returned an empty `relation_paths` with **no warning**. Now follows routes and warns `RELATION_PATH_UNRESOLVED` on a shortfall; 0.3182 → 0.5000 with no other metric moving | none (post-gate) |

| [0035](0035-relation-endpoint-naming.md) | The corpus declared relation endpoints as bare module names — `orders EXPORTS Order` — that **name no symbol**; the module symbols are `src.orders`, `src.client`, `src.payments.service`. Qualified, on the ADR-0031 test: an expectation must reference an identifier the system can produce. 0.5000 → 0.6364. **q010 is deliberately half-fixed** — its target is a modelling question (does `IMPORTS` point at the module or the class actually bound?), not a spelling, so it still scores 0 for one stated reason | none (post-gate) |

| [0036](0036-expectations-name-real-symbols.md) | ADR-0031 and ADR-0035 each found, by hand, an expectation naming a symbol that does not exist — a failure **no metric can catch**, since a metric only scores what it is given. Now asserted in the suite against the engine's own `find_exact`. It immediately found **q024 still carrying the pre-ADR-0031 convention**, invisible because its `CONCEPTUAL` intent is unmeasured; mutation-checked by reintroducing both historical defects | none (post-gate) |

| [0037](0037-pid-reuse-detection.md) | A pid is a slot the OS reassigns, not an owner — so a dead run whose pid was reissued looked alive and blocked its repository from reindexing **permanently**, an item declared open at the Phase 6 gate and carried through Phase 7. The stamp now records the owner's process *start time* too. The stated blocker ("no portable source without a new dependency") was half right and the wrong half kept it open twelve days: there is no portable source, but `GetProcessTimes` sits beside the `OpenProcess` this module already calls, and Windows is the supported environment. `None` reads as alive, and a stamp without the key keeps the behaviour it was written under | none (post-gate) |

| [0038](0038-relation-path-recall.md) | `relation_path_correctness` scored **precision**, so every true edge the engine emitted that the corpus did not declare lowered it — and **ADR-0020 requires emitting every supporting edge.** The measurement punished the engine for obeying an accepted decision. ADR-0034 and ADR-0035 each named the symptom (q005, q015 capped at 0.5) without naming the instrument. `relation_path_recall` added beside it at 0.6364 → 0.7273, precision retained so no baseline changes meaning. **Deliberately ungated** — two of ADR-0034's four causes are still open. The fifth instance of the apparatus, not the engine, being wrong | none (post-gate) |

| [0039](0039-imports-targets-the-bound-symbol.md) | The modelling question ADR-0035 deliberately left half-fixed: does `IMPORTS` target the module or the class the statement binds? **The class** — `from x import Y` binds `Y`, and ADR-0021's import-and-call rule requires a *class* owner precisely so `import orders` cannot vouch for every symbol inside it. The decisive fact was that **q010 contradicted itself**: its `expected_symbols` already named `IdempotencyStore` while its relation string said `idempotency`. Correctness 0.6364 → 0.7273, recall 0.7273 → 0.8182, nothing else moved. One of ADR-0034's four causes now remains | none (post-gate) |

| [0040](0040-ephemeral-scope-is-the-server.md) | The scope question carried since 2026-08-09, closed as **won't-fix with the reasoning recorded**. Ephemeral means storage discarded on process exit and a CLI command exits at once, so every invocation would get its own empty database — the `repo add` that registered a repository invisible to the `index` that followed it, and the documented CLI workflow structurally impossible. The half that *was* a defect (nothing saying which database was in play) was already fixed. No behaviour change; two mutation-checked tests now pin both sides of the boundary | none (post-gate) |

| [0041](0041-nested-config-keys-hash-their-own-value.md) | Reported from a real preflight run: **one `version` edit in `pyproject.toml` reported eight configuration keys changed, seven falsely.** ADR-0025 rightly gave a leaf whose line cannot be located its *parent's* range so a citation is never invented — but `content_hash` was taken over that range, so the leaf hashed the whole parent block and any edit inside marked every such key modified. The range still cites; the hash now covers the key's own value (parsed value for JSON/TOML, subtree text for YAML, path included so equal values stay distinct). 8 findings → 2. `PARSER_BUNDLE_VERSION` 1.3.0 → **1.4.0**; every snapshot stale. **ADR-0025 measured index volume and never asked what the fallback does to change detection** — the retrieval side was measured, the change side is the wedge | none (post-gate) |

| [0042](0042-a-symbol-pairs-within-its-file.md) | The other half of the duplicate report, and **not** the UI issue the register guessed at. `symbol_diff` grouped by `(kind, qualified_name)` with no file in the key, so a config key name in *N* files was an *N*-versus-*N* match that fell to the ambiguous branch and reported `2N` changes — **four findings on a clean working tree with byte-identical content**. Occurrences now pair within their file first; the cross-file move rule survives what is left over. A config key is also folded into the descendant that changed, on its **dotted path** (line ranges reach only the top level after ADR-0041), and a derived `DOCUMENTS` edge targets the key it names, as ADR-0039 did for `IMPORTS`. Clean tree 4 findings → **0**; one nested edit 4 → **1**; the `pyproject.toml` bump 8 → 2 → **1**. `RESOLVER_VERSION` 1.3.0 → **1.4.0**; every snapshot stale. Two corpus expectations gave a leaf its *parent's* range and were corrected — and doing so exposed that **c012 had emitted the user's duplicate since Phase 4**, invisible because `expected_findings` is a set of codes and a set cannot count | none (post-gate) |

| [0043](0043-line-endings-are-not-a-change.md) | Found verifying ADR-0042 on a real repository instead of a fixture: an **unmodified** checkout still reported **150 findings across 35 files** while `git status` was empty. The base view reads the blob, the target reads the working tree, and Git rewrites line endings between them whenever `core.autocrlf` is on — **the Windows default**. Both sides hashed raw bytes, so every line differed and **Git and CodeAtlas gave opposite answers to "did this file change?"** Both sides now normalize CRLF and lone CR to LF, for the compared hash *and* the bytes handed to the parser — agreeing at file level while parsing different bytes would only move the disagreement down to every symbol. A real edit still counts; binaries are excluded; `SnapshotStateView` is deliberately untouched because nothing pairs it with another view. 150 → **26**. No schema, contract, or version change. Every baseline reproduces byte-for-byte, because the corpus is LF on both sides and **could never have seen this** — the fourth such blind spot | none (post-gate) |

| [0044](0044-preflight-sees-only-what-it-would-index.md) | The item ADR-0043 left open, and the last of the original 1592: the two views disagreed about which files **exist**, not about their bytes. `GitBlobStateView` lists everything tracked at the ref while `DirectoryStateView` lists what a scan would index, so a file that is tracked **and** excluded from a scan was present in the base, absent from the target, and reported `SYMBOL_DELETED` at **high** severity — `overall_risk` `high` on an unmodified checkout. Ruled by the user: **preflight never considers a file it would not index**, so the blob side now applies the same ignore rules and the same content-based binary sniff (`is_binary_content`, made public so two implementations cannot drift apart). 12 base-only files → **0**; 26 findings → **0**. The rejected alternative was letting tracked files bypass ignore rules, which would pull built output and minified bundles into the index through a comparison. **The oversized-file case is deliberately excluded**: it does not disagree, it *raises*, so one tracked 3 MB file makes a repository impossible to preflight while the scanner merely skips it — a worse defect, a separate ruling, now pinned by a test asserting today's behaviour and recorded in the register. Fifth consecutive defect the corpus could not see, and `test_git_blob_state_view_lists_same_paths_as_directory_view` has asserted this exact property **since Phase 4** while passing throughout, because its fixture contains nothing any rule excludes | none (post-gate) |

| [0045](0045-an-oversized-tracked-file-is-skipped-and-declared.md) | ADR-0044 aligned three of four exclusion mechanisms and deliberately left the fourth: an oversized tracked file did not *disagree*, it **raised**, so one committed 3 MB CSV made a repository impossible to preflight while the directory scan merely skipped the same file. Ruled by the user: **skip it, and declare the omission**. `archive` skips and names oversized entries; `read_blob` still raises, because it is asked for one specific blob and answering "here is nothing" would be worse; `StateView.excluded_files()` carries the names and the engine turns them into a `FILE_TOO_LARGE` warning and a limitation. The directory side now declares its skips too — it had recorded `TOO_LARGE` since Phase 1 and never surfaced it, so that half was already silent. Only oversize is reported: an *ignored* file stays unreported, because ADR-0044 already ruled it outside the index. The pinning test is inverted rather than deleted | none (post-gate) |

| [0046](0046-a-module-can-answer-a-conceptual-question.md) | The product question ADR-0030 left open: when a concept is documented at module level and the corpus declares the method implementing it, is the module a correct answer? **Yes**, and **no ranking change is made** — the coarse-chunk penalty contemplated by ADR-0028 and ADR-0030 stays unimplemented, so the rank-1 containment hit s001 currently produces is preserved. Ruling the other way would have declared a defect where an investigation found none, spent real risk on a metric ungated on this profile (ADR-0023), and traded an evidence hit for a symbol hit — inverting the product's own priority. No code change; WS-5 becomes a measurement rather than a fix | none (post-gate) |

| [0047](0047-graph-evidence-is-the-reference-site.md) | For a graph intent the evidence is the **reference site** — the line that proves the claim — not the definition range of the symbol the answer is about. An **absent decision, not a faulty instrument**: the engine had consistently cited reference sites and the corpus had consistently declared definition ranges in eight cases, each internally coherent, with no ruling ever taken. Eight expectations corrected; `_contains` is **not** loosened to accept overlap, because that would move the number without settling anything. `EXPORTS` keeps ADR-0019's rule, where the reference site *is* the exported symbol's definition | none (post-gate) |

| [0048](0048-containing-evidence-rate-is-reported-not-gated.md) | `containing_evidence_rate` is **reported, never gated**, rather than having its threshold lowered. A rate whose 1.00 target is unreachable while the engine obeys ADR-0020 — which requires every graph answer to cite every supporting edge — punishes compliance. Lowering the threshold instead was refused: a number chosen to be passed says less than it appears to (ADR-0032, ADR-0033). The metric keeps being computed and published so no tracked baseline changes meaning; `containing_evidence_recall_at_10` keeps the 0.90 gate | none (post-gate) |

| [0049](0049-a-fixture-re-includes-a-directory-an-ignore-default-excludes.md) | **Recorded 2026-08-21, five days late, because ADR-0047 cites it by number and it did not exist.** The `git_changes` fixture's `target/` side is also a build-output ignore default, so a *query* case — which indexes the fixture root rather than one side — could not see half the fixture, and **q034's recall was structurally 0 and always had been**. Re-included with a fixture-local `.codeatlasignore` (`overrides=True`), leaving the builtin default untouched for every real repository. Renaming was rejected because `_resolve_side` matches the literal names `base` and `target`; marking the cases unmeasured was rejected because it would make ADR-0024's signal mean two things. **Cost paid immediately: it put a second `process` in the index, so q035 became ambiguous and abstained** — which is what ADR-0050 then settled. Change cases verified byte-identical before and after | none (post-gate) |
| [0050](0050-q035-declares-its-subject-and-its-reference-site.md) | q035 declares `query_subject: target.processor.process` and its evidence becomes the reference site `4-4`, restoring `exact_symbol_resolution` to 1.0000 from a zero-margin 0.9800. Two findings recorded: the plan's stated blocker was **wrong** — `find_exact` tier 2 accepts `module_path.qualified_name`, and the ambiguity message is unusable because it prints two identical `qualified_name`s; and the subject fix alone **passed its own wrong-side mutation**, so it restored the number without restoring the measurement. The reference-site correction is what makes the case discriminate. Takes 0050, not 0049, which ADR-0047 forward-references for a record never written | none (post-gate) |

| [0051](0051-q006-is-mistyped-not-defective.md) | q006 was carried as the only candidate *engine* finding in nine investigations, and it is not one. Its `TRACE_FLOW` routes to a channel that **structurally cannot** cite line 7 — `return "duplicate"` contains no relation, and under ADR-0047 a graph answer cites reference sites — while the product's own classifier sends that question to `text`, whose result contains line 7. Re-typed to `CONCEPTUAL`, with **q064 added in the same change** because `TRACE_FLOW` is a symbol intent and removing q006 would drop the denominator 50 → 49, where one miss scores 0.9796 and fails the gate. Records three ways a case can score without measuring what it claims | none (post-gate) |

| [0052](0052-a-claim-may-not-outrun-its-citation.md) | **An engine defect**, found while investigating ranking sensitivity rather than by looking for it. Traversal runs to `max_depth` 2, so a graph answer routinely holds edges touching **neither end of the root**, and `_claims` rendered them against the root's name anyway — "test_x calls IdempotencyStore.claim" for a test that calls `capture`, citing the line where it calls `capture`. `relation_paths` carried the true two-step path throughout; only the prose collapsed it. A claim beyond the first hop now reads "reaches Y indirectly, through Z", mirroring ADR-0016's mediation wording. Prose only: no contract, derivation, or answer-set change, and every baseline reproduces byte-for-byte — which also means **the corpus cannot see it** | none (post-gate) |

| [0053](0053-a-gated-intent-leaves-the-denominator.md) | `CONCEPTUAL` was missing from `SUPPORTED_INTENTS`, so **q024 was never measured** and ADR-0051 moved q006 into the same state hours earlier. ADR-0017 on the neighbouring constant — the one ADR-0017 itself said "*was* maintained" — and it fails the **opposite** way: a gated *fixture* scores `False` and stays in the denominator as a miss, while a gated *intent* scores `measured=False` and **leaves** it, removing a failing case from the average. Under-reporting is loud; flattering is silent. Four metrics fall to the truth (`relation_path_recall` 0.9130 → **0.8750**), no gate breaks, and a corpus-derived guard stops the constant drifting again. **Corrects ADR-0051's evidence while confirming its conclusion** | none (post-gate) |

| [0054](0054-a-finding-says-what-it-is-about.md) | The recorded task was a rendering problem — two legitimate findings sharing a code render identically and collide on a React key. The **symptom**. `_finding_citations` keyed changed symbols on `qualified_name` alone, so two modules each defining `total` collapsed to whichever the dict saw last and **the finding about one file cited lines in the other** — a §4.1 violation, and ADR-0042's "pair within the file first" reaching a surface that ruling did not touch. `FindingDraft` now carries its file and the citation resolves by location only, with no name fallback. `Finding` gains optional `subject`/`file_path`, **derived from the citation rather than stored**, so no migration was needed and a reloaded report cannot disagree with the fresh one. All four renderers plus JSON and the web list; SARIF keeps its own `artifactLocation` | none (post-gate) |

| [0055](0055-a-route-cites-the-handler-it-reaches.md) | A resolved `ROUTES_TO` edge additionally cites the **handler's definition**, an explicit exception to ADR-0047 on ADR-0019's `EXPORTS` precedent — a route *names* its target, and unlike an export its literal and target sit in different files and usually different languages, so the near side alone cannot show what the flow reaches. Reproducing it found a defect: evidence is deduplicated by region, but claims were built from the surviving pairs, so **the route's claim was dropped whenever it shared a line with the call carrying it** — the engine kept two unresolved browser globals and discarded its only resolved cross-language edge, by iteration order. `containing_evidence_recall_at_10` reaches **1.0000**. Two of four mutations could not be exercised by the fixture and that is recorded, not counted as coverage | none (post-gate) |

| [0056](0056-the-coarse-chunk-penalty-is-a-pure-loss.md) | The measurement ADR-0030 demanded before any ranking change, and the answer is **don't**. A granularity penalty was applied corpus-wide at three strengths and **every metric that moves, moves down** — including `symbol_recall_at_10` 0.9286 → 0.8571, so ADR-0030's predicted *trade* is not a trade but a loss on both sides. The reason is a case ADR-0030 did not anticipate: s013's expected answer `OrderStatus` **is itself a class chunk**, so the penalty demotes the answer it exists to promote, 7 → 28; s001 loses its only containment hit, 1 → 11; s007 gains one rank on a case already inside the top 10. Also records that the task's premise was false — the corpus fusion runs on is **14 cases, byte-identical since 2026-07-31**, because WS-1 grew a corpus `_fuse` never touches — and that ADR-0046's "cheaper" widening option is **not available** for s001, which names a symbol the engine does produce. ADR-0028's s004 regression no longer reproduces; deliberately not attributed | none (post-gate) |

| [0057](0057-a-lexical-answer-carries-its-resolved-edges.md) | **The last of ADR-0034's four causes.** A lexical or conceptual answer emits relation paths, restricted to **edges that resolve to a real target** — ADR-0055's precedent, and structural rather than stylistic: `target_symbol_id` is set for no state except `RESOLVED`, so an unresolved edge has no far endpoint and cannot form a path. Eight of `Order flow`'s ten `DOCUMENTS` edges point at prose words ("order", "flow", "requests"), and emitting those would turn a wording coincidence into a relationship. Claims stay `high_confidence_heuristic`, a step carries the *edge's* derivation, and a step cites evidence the answer **already returned** — so `containing_evidence_rate` holds at 0.7537, the signature of adding no evidence row. `relation_path_recall` **0.8750 → 1.0000**, correctness 0.7917 → 0.8646, nothing else moves. **The precision prediction failed and that is recorded**: `_precision` returns 0.0, not 1.0, when nothing is predicted and something is expected, so all three cases were scoring zero on both. Also records what the metric **cannot** see — q006 and q031 now emit true undeclared edges, and the aggregate excludes cases declaring none | none (post-gate) |

| [0058](0058-relation-path-recall-is-gated-at-one.md) | `relation_path_recall` is gated at **1.0, absolutely**, discharging the deferral ADR-0038 recorded and its "that design decision is settled" trigger. Absolute for ADR-0032's reason: this is a **deterministic emission** question, not a ranking one — an edge the corpus declares, the store holds and resolution has bound either appears or it does not, and no tolerance has a named cause. The denominator is **24** and **not uniform** (21 cases declare one edge; one each declare two, three and five), so the reachable values below 1.0 are 0.9917, 0.9861, 0.9792, then 0.9583 — making 0.99 privilege one case by accident of its edge count. Registered on the **`retrieval` profile only**, which is load-bearing: the semantic corpus declares zero relations, its aggregate is `None`, and `_unmet_targets` counts `None` as a miss, so a shared entry would have failed the Phase 7 gate on a metric that corpus cannot express — ADR-0023's mistake exactly. **Mutation-checked**, because a threshold the corpus already satisfies is decoration: regressing ADR-0057 drove recall 1.0 → 0.875 and the gate caught it | none (post-gate) |

| [0059](0059-an-expectation-declares-direct-results.md) | **A graph expectation declares direct results only**, settling what blocked Task 6. Indirect results may still be returned — ADR-0052 already makes the engine label them "reaches Y indirectly, through Z" — and leaving them undeclared is what makes `exact_symbol_resolution` a **ranking** gate rather than only a resolution one: with a true indirect result present, the metric asks whether the *direct* answer ranks first, and a reversal breaks it. **Two of the task's three premises were wrong.** The counts had drifted (recorded "9 sensitive, 9 distractors, the same 9"; measured **11, 12, and not the same set**, partly from ADR-0051/0053/0057), and "fixing them takes coverage to zero" is false — correcting q015 leaves the depth-2 `Order` and it stays sensitive. The claim that sensitivity is *structurally unavailable* is **refuted**: it needs a two-hop chain, and `symbol_breadth` simply had none. q015 corrected on ADR-0036 grounds (`client.js` **defines** `render`, imports only `total`); the fixture gains `start_pipeline` for a two-hop chain; q065 queries it. **q053 becomes the first post-2026-08-15 case to be reversal-sensitive.** Denominator 50 → **51**, checked before the case was added: one miss scores 0.9804 and still clears 0.98 | none (post-gate) |

| [0060](0060-symbol-diff-groups-relations-once.md) | **A quadratic stage nobody had recorded.** Measuring the register's ">15 minutes on a 664-file repository" confirmed the documented O(repository) cost (8× the files → 7.12× the time, exponent 0.95) and found one stage that did not scale like the rest: **`symbol_diff` at exponent 2.02**. `_dependency_changed` and `_binding_span` each filtered the *whole* relation list for one symbol's edges, and both run once per surviving symbol pair — O(symbols × relations). **The worst case is an unchanged repository**, since the dependency check runs precisely when a content hash is *un*changed, so the cost is paid on every preflight and a bigger diff makes it cheaper. Relations are now grouped by `source_symbol_id` once, as `_group_by_key` already does for symbols: **768.8 ms → 19.7 ms at 800 modules (39×), exponent 2.02 → 0.78**, share of engine time 18.8% → 0.6%. Guarded by a **scan-counting** test rather than a timing one — before the fix it read 51 traversals for 50 symbols, 401 for 400 — with a second test pinning *growth* so raising the ceiling cannot defeat it. All three baselines reproduce byte-for-byte. **But it is not the fix that matters, and the record says so.** Measured on CodeAtlas itself the register's ">15 minutes" reproduces at **635.59 s**, and **parsing is 99.5% of it** (`parse_base` 316.2 s + `parse_target` 316.0 s) while `symbol_diff` is 246 ms — so this change removes about **2.7%** of the real-world time. The synthetic profile measures *scaling* honestly and *proportions* dishonestly: it made `file_diff` look like the leading stage, which is false on a real repository. `change-analysis.md` was right all along. Also corrects three record errors: the "snapshot-reuse path" was attributed to ADR-0005 decision 2 (that is Git security posture; it is decision 1), `SnapshotStateView` is **unused rather than unimplemented** — four tests, zero production callers — and wiring it in **would not have fixed this**, since its `read_file` still reads and hashes every file | none (post-gate) |

| [0061](0061-an-unchanged-file-is-parsed-once-per-analysis.md) | The two sides of a change share every file it did not touch, and every field of the engine's `ParseRequest` derives from `(relative_path, language, content)` — `repository_id` and `snapshot_id` are the same constant on both sides — so an unchanged file built a **byte-identical** request twice and one of two identical answers was discarded. Now cached for the duration of one `analyze()` call: **606 → 305 parser calls** on a 303-file preflight, against a theoretical minimum of ~304. **The headline is a count, not a clock**, because the machine moved 343 s → 549 s on an untouched path mid-session and made wall-clock comparison worthless. Scoped to the call deliberately: same process, same bytes, so reuse is correct by construction and the invalidation ruling the register asked for is not needed. **Corrects ADR-0060**: its "99.5% is parsing" is 99.5% *parse plus resolve* — those timers wrap resolution too, 766 ms of 2137 ms on one profile — which is why the pre-registered prediction that wall time would halve **was wrong**, and why `parse_target` now reports order rather than effort. Explicitly claims **no** end-to-end speedup: mechanism confirmed, payoff unquantified | none (post-gate) |

| [0062](0062-resolution-is-not-cached.md) | **Asked for, measured, declined — with the reason.** Caching resolution needed three things to be true and none is: it is **linear** (fitted exponent **1.14**, no `symbol_diff`-shaped defect), it is a **modest share** (~127 ms of the 477 ms under the parse timers, ~**6% of preflight**), and it is **not cacheable in the shape parsing was** — parsing is per file and the two sides share every untouched file, while `resolve()` takes the **whole state**, so a key over it cannot hit within a call by construction. Making it incremental would contradict a load-bearing property, not merely add risk: adding a symbol can make a reference in an **unchanged** file resolve, so an incremental resolver must compute the blast radius the engine exists to answer. **Corrects ADR-0061**, whose "766 ms of 2137 ms" was measured under the very machine load that record warns about and summed **three** resolve calls when only two sit inside those timers. The narrower lesson: ADR-0061 used that number to *explain a failed prediction*, which is where a wrong number does most damage, because the story closes and nobody re-measures | none (post-gate) |

| [0063](0063-index-reuse-cannot-lower-the-parse-count.md) | **The remedy three records deferred to, declined on arithmetic rather than on the trust ruling they were waiting for.** The index turns out to supply more than expected — there is **no references table**, but `GraphSide`'s four fields are all stored, so a side could be rehydrated with **no migration**. It still cannot help: only the *target* is in the index (`analyze_working_tree` refreshes first), the base is a Git commit that is not, and since ADR-0061 each unique file is parsed **exactly once** — 305 parses for 303 files — with that one parse serving both sides. Rehydrating the target removes its *use* of the parse, not the parse, because the base still needs it. **Saving: ~2 parses.** For commit-range analysis it is exactly zero, since neither side is the active snapshot. Records what was avoided: re-keying stored IDs into the analysis space, where ADR-0042 pairs symbols **within their file** by `file_id`, so a synthetic-ID base against a real-ID target would pair nothing and **report every symbol as deleted and re-added** on the core workflow — plus silently dropping `PARSE_FAILED_TARGET`. The trust ruling belongs to a *persisted* cache, which outlives the process and genuinely needs version keys | none (post-gate) |

| [0064](0064-resolution-indexes-what-it-used-to-scan.md) | **The bottleneck, found in the stage three records had already measured and dismissed.** Timing `_analyze_state`'s four operations separately instead of trusting a timer's name: `list_files` 1.25 s, `read_file` 0.07 s, **`parse` 8.14 s (2.5%)**, **`resolve` 310.24 s (97.0%)**. Parsing the whole repository takes **8 seconds** — so ADR-0060's "99.5% is parsing", ADR-0061's inherited framing, and ADR-0062's "~6% and linear" are **all corrected here**. `resolution.py` had claimed O(references) since it was written while three call sites scanned `symbols_by_id.values()` per reference: `_resolve_mention` (112,265 mentions x 11,502 symbols = **1.29 billion**, `str.lower` called 880 million times), `_RouteIndex.handlers` (`name_tokens`, a regex split, called **10.5 million** times), and `_derive_config_edges`. ADR-0062's exponent of **1.14** was honest and useless: it was fitted on a generated corpus of ~15-line Python modules containing **no Markdown, so no mentions and no routes** — the entire quadratic term was structurally absent from the sweep. Fixed by moving every symbol-only predicate into `_Index`, plus `matching_forms`, which enumerates the closed plural rule so token matching becomes a dictionary hit instead of a comparison against every symbol. **Verified identical across all 168,605 relations on the real repository**, not merely green on fixtures. Guarded by a *counting* test, because wall-clock is exactly what sent the previous three records wrong | none (post-gate) |
| [0065](0065-query-backed-language-support.md) | Query-backed parser engine with thin per-language adapters, applied to **Java, Go, Rust and Scala only** — symbols, `IMPORTS`, `CALLS`, `INHERITS`, `IMPLEMENTS`, and changed-symbol detection, but **no test edges and no route detection**. Grounded in a 2026-08-19 spike: nine of eleven grammars ship a `tags.scm`, none of them captures an import, and Go's receiver is a node field rather than a lexical ancestor — which disproves a purely declarative design twice over | none (post-gate) |
| [0066](0066-a-go-import-is-declared-external.md) | A Go import stays `external`, permanently. The module prefix lives in `go.mod`, which a single-file parse cannot read, so closing it needs a *matching policy* rather than more parsing — and the cost is asymmetric: trimming too far makes a third-party `github.com/foo/payments` resolve onto a local `payments`, **inventing** a relationship §4.1 forbids. A miss is the safe direction. The `strict` xfail is **inverted, not deleted** (ADR-0045's precedent), so the ruled behaviour is pinned in the place that used to predict a fix | none (post-gate) |
| [0067](0067-a-profile-may-author-a-second-query.md) | `LanguageProfile` gains an optional `references_query`, and Scala supplies one. Its shipped `tags.scm` matches only `(call_expression (identifier) @name)`, so `payments.charge(id)` — most real Scala — produced no edge; Java, Go and Rust all ship a member-call pattern. **Unlike ADR-0066 the information was present**, and only a query was missing, which is why one is declined and the other closed. `PARSER_BUNDLE_VERSION` **1.5.0 → 1.6.0**; `RESOLVER_VERSION` deliberately unchanged | none (post-gate) |
| [0068](0068-a-message-scoped-route-is-addressed-as-a-message.md) | Two `AGENTS.md` §12 divergences, ruled together. **§12.2: move the code.** `retry` and `feedback` sat under `/v1/conversations/` while carrying **no conversation id** — the prefix was inherited from the file the handlers landed in, not chosen — and `cancel`, the third operation on the same run lifecycle, already sat at the top level, so the implementation disagreed with itself along the axis of which file a handler was written in. Neither route had a Python test and `feedback` had no caller at all, which is why nothing objected. **§12.3: remove `POST /v1/query/stream`** — specified in Phase 0, never built, never missed; a documented endpoint that does not exist is the `SECURITY.md` version table again. Breaking, and approved as one: loopback-only, no releases, one generated-type client. `contract_version` stays `1.1` deliberately — where an operation is addressed changed, the payload did not | none (post-gate) |
| [0069](0069-a-symbol-is-identified-beyond-its-name.md) | Two symbols sharing a file, qualified name and kind collapsed onto one `symbol_id` and **indexing failed outright** -- found by indexing real repositories, latent since Phase 1, six of seven languages. Identity is disambiguated per file; `qualified_name` is deliberately left alone, unlike the TS/JS prior art, so an overload stays findable by the name a caller types. No reindex: the first member keeps its id | post-gate |
| [0070](0070-an-import-is-attributed-to-both-its-file-and-its-class.md) | An import is attributed to the compilation unit **and** to the file's first definition -- both edges are true. Per-case measurement disproved the recorded cost: only 3 of 80 cases moved and all three *abstained*, so the damage was a lost class edge, not the ranking dilution the record claimed. `PARSER_BUNDLE_VERSION` 1.6.0 -> 1.7.0, reindex required | post-gate |
| [0071](0071-a-signature-separates-overloads-and-nothing-else.md) | Java and Scala emit a `signature` -- parameter **types** only, so a rename cannot change identity -- and Go and Rust deliberately emit `None`, having no overloading. Measured on five real repositories: it separates **221 of 1202** collision groups (**18.4%**), not the "four languages" the ADR-0069 follow-up claimed. `PARSER_BUNDLE_VERSION` 1.7.0 -> 1.8.0, reindex required | post-gate |

| [0072](0072-what-adr-0071s-remaining-collisions-actually-are.md) | DR-01's census reproduces ADR-0071's 1202/221/981 **exactly** and disproves its account of them: a Scala `trait`/`object` pair never collided (different kinds), their *members* do, and gson's 47 "no remedy" groups are enum-constant bodies, anonymous classes and method-local classes -- all separated by the enclosing scope. Three mechanisms covering 934 become **two covering 980 of 981**. The instrument is committed this time, so the next claim can be run instead of reasoned | post-gate |
| [0073](0073-four-evaluation-conventions-ruled.md) | Four corpus conventions the engine cannot settle for itself, ruled by the user: a caller expectation **may** name its own subject and the recall cost is accepted; a case declaring no relations leaves the relation-precision denominator; **traversal depth becomes part of each case**; and `TRACE_FLOW` is **audited before it is ruled**, because the outcome may be a corpus limit or an engine defect and those have opposite remedies. A fifth -- what invalidates a stored parse -- was **withdrawn** rather than put to the user, ADR-0063 and ADR-0064 having left the change it authorises paying nothing | post-gate |
| [0074](0074-a-symbol-is-identified-by-the-declaration-it-sits-in.md) | `LanguageAdapter.discriminator`: the declaration a symbol sits inside, or its own form. Appended to the id hash **only when non-empty**, so a language moves for its own reason. **198 groups gained position-independent identity where ADR-0072 predicted 980** -- that record classified by *sampling* and never checked whether the enclosing declaration actually differs between colliding members; usually it does not. Third time in this lineage an inherited count was the defect. `PARSER_BUNDLE_VERSION` 1.8.0 -> 1.9.0, reindex required | post-gate |
| [0075](0075-a-graph-case-declares-its-traversal-depth.md) | Depth was implied: `max_depth` defaults to 2 and every graph case took it, while ADR-0059 ruled an expectation declares **direct** results -- so undeclared second-hop results read as distractors, which is what makes `exact_symbol_resolution` a *ranking* gate. Every graph case now declares **2**, the value it already had. Measurement says all 31 satisfy their relations at depth **1**, and depth 1 is **rejected**: it would raise precision by deleting the corpus's only distractors, ending its reversal coverage and turning a ranking gate back into a resolution one. Both baselines reproduce **byte-for-byte**, which is the evidence this changed no answer | post-gate |

| [0076](0076-a-case-that-emits-relations-declares-them.md) | ADR-0073 ruling 2's premise was **already false**: `runner.py` had excluded cases declaring no relations since before the ruling, so "exclude them" was the status quo and could move nothing -- and the options carried each other's consequences, since `_precision(predicted, empty)` returns **0.0**, making *keeping* such a case the thing that would expose broadening. Re-put to the user, who chose the third option: a case whose answer carries relations **declares** them. q003, q006 and q031 gain seven edges, **read off the fixture source rather than transcribed from the engine** (ADR-0003; the engine then agreeing is a result, not the method). `relation_path_correctness` 0.8932 -> **0.9024** purely because the denominator grew by three cases -- **not an engine improvement**, and recorded so it cannot be read as one (ADR-0053). Recall holds at 1.0, so ADR-0058's absolute gate stands | post-gate |

| [0077](0077-the-corpus-grows-a-shape-it-could-not-express.md) | Two register rows recorded the same class of gap: a defect **the corpus is structurally unable to see**. `ChangeCase` gains `base_view`, so a case can read its base through `GitBlobStateView` -- the view a real preflight uses, and the only shape in which ADR-0044's ignore-rule fix participates at all. Two directories could never have shown it: both sides apply the same ignore rules, so a case would pass **with the fix and without it**. c033 discriminates -- remove the filter and it reports `bundled_total` deleted at high severity. `coverage/` was used rather than `dist/`, which this repository's own `.gitignore` excludes (ADR-0049's collision, met again). The semantic corpus also gains a **second fixture**, 14 cases over one becoming 18 over two. `changed_symbol_precision` 0.9531 -> **0.9545 is dilution, not improvement**; the honest counterpart `changed_symbol_exact_cases` 29 -> **30** is the one that cannot be lifted by adding passing cases | post-gate |
| [0078](0078-a-semantic-artifact-declares-the-corpus-it-was-measured-on.md) | The register asked whether the `-Semantic` `--check` steps should stop being opt-in. **Refused, with its reason**: gate condition 2 requires an installation without the optional extras to behave exactly as Phases 0-6, and making that block mandatory would make the deterministic gate depend on torch -- the exact regression the condition exists to catch. The row was pointing at something narrower. Both recorded staleness incidents shared one signature (added metric keys, no value change) and DR-01b's guard covers it; what it cannot cover is an artifact stale because **its inputs moved**, which is what ADR-0077 did by adding a whole semantic fixture. Inputs hash **without installing anything**: `dataset_inputs_digest()` covers manifest, cases and every fixture byte, both generators stamp `corpus.inputs_digest`, and the guard runs in every gate. Mutation-checked against the real scenario -- one appended line to a fixture fails both artifacts while the key-set guard stays green. Does **not** close drift from a model or library upgrade, which still needs `-Semantic`, and says so | post-gate |

| [0079](0079-a-subject-is-a-symbol-not-a-sentence-fragment.md) | Trailing sentence punctuation is stripped from an extracted subject -- `trace the flow from mount.` yielded the subject `mount.`, reached the right channel and answered **nothing**, across all seven subject-bearing rules. The `TRACE` rule is **not** widened: ruled, then declined on measurement, because no subject extractable from q032's question resolves and routing it would move the case from one of two expected symbols to zero. The brief's "reaches 4 of the 63" is corrected to **1** | post-gate |

| [0080](0080-the-release-gate-measures-the-profile-it-can-reproduce.md) | The Phase 4 p95 targets stay gated on the **synthetic** profile, which ADR-0064 showed cannot contain the dominant cost; the realistic figures (refresh first misses at 80 modules, preflight at 300) are declared in `AGENTS.md` §19.3 rather than absorbed. Option (b) rejected on timing — it opens a red gate at 80 modules during a closeout; option (c) stays refused by ADR-0048 | post-gate |
| [0081](0081-the-packaged-build-ships-the-mcp-server.md) | The packaged build ships **two** executables -- `codeatlas.exe` and `codeatlas-mcp.exe` -- in one shared bundle. The artifact contained no MCP entry point at all, so an agent using the release could not use MCP; it was source-checkout-only, and the README listed its 22 tools without saying so. Forced the build from a command line to a spec, because `pyinstaller a.py b.py` builds one program over two scripts. Verified by speaking the protocol, since a stdio server cannot answer `--help` -- and `--help` is what still worked when ADR-0065 destroyed the artifact | none (post-gate) |

## Notes

None is superseded. ADR-0008 is the first record to change a published contract
under Section 25, and carries that section's checklist as an explicit table.
ADR-0068 is the second, and the first to do so *without* moving
`contract_version`: it relocates where two operations are addressed while
leaving every payload untouched, which is the distinction that version is for.
ADR-0009 admits the optional vector store the blueprint gates behind its
activation approval, and records why no Section 25 item is triggered by
default.
